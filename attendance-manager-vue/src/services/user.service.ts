import { CreateUserParameters } from "@/modules/user/index";

export default class UserService{
    //TODO in progress
    static CreateUser(user: CreateUserParameters): boolean{
        return true;
    }
}